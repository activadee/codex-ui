name: Go Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    uses: activadee/codex-shared-workflows/.github/workflows/go-tests.yml@main
    with:
      pre_test: mkdir -p frontend/dist && touch frontend/dist/.placeholder
